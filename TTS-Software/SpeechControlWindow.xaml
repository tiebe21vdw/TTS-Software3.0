<Window x:Class="TTS_Software.SpeechControlWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Voorlezen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        SizeToContent="Height"
        Width="440"
        WindowStartupLocation="Manual">

    <Border Background="{DynamicResource PanelBrush}" CornerRadius="16" Padding="16,14">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.45" Color="Black" />
        </Border.Effect>

        <Border.Resources>
            <Style TargetType="Button">
                <Setter Property="Width" Value="34" />
                <Setter Property="Height" Value="34" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="ButtonBackground" Background="Transparent" CornerRadius="17">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="ButtonBackground" Property="Background" Value="{DynamicResource HoverBrush}" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </Border.Resources>

        <StackPanel>
            <!-- Live meelezende tekst: het huidige woord wordt gemarkeerd naarmate de stem het uitspreekt -->
            <ScrollViewer x:Name="TextScrollViewer" MaxHeight="96" VerticalScrollBarVisibility="Auto" Margin="2,0,2,12">
                <TextBlock x:Name="CaptionText" TextWrapping="Wrap" Foreground="#DDFFFFFF" FontSize="14" LineHeight="21" />
            </ScrollViewer>

            <StackPanel Orientation="Horizontal">
                <Button x:Name="PlayPauseButton" Click="PlayPauseButton_Click" Margin="0,0,4,0"
                        ToolTip="Pauzeren / hervatten">
                    <TextBlock x:Name="PlayPauseGlyph" Text="&#10074;&#10074;" FontSize="14" Foreground="White" />
                </Button>
                <Button x:Name="StopButton" Click="StopButton_Click" Margin="0,0,16,0" ToolTip="Stoppen">
                    <TextBlock Text="&#9632;" FontSize="14" Foreground="White" />
                </Button>

                <TextBlock Text="Volume" Foreground="#99FFFFFF" FontSize="12" VerticalAlignment="Center" Margin="0,0,8,0" />
                <Slider x:Name="VolumeSlider" Minimum="0" Maximum="100" Width="130"
                        VerticalAlignment="Center" ValueChanged="VolumeSlider_ValueChanged" />
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
