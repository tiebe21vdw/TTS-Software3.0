<Window x:Class="TTS_Software.ProcessingIndicatorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Bezig..."
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="Manual">

    <Border Background="{DynamicResource PanelBrush}" CornerRadius="22" Padding="14,9">
        <Border.Effect>
            <DropShadowEffect BlurRadius="18" ShadowDepth="0" Opacity="0.45" Color="Black" />
        </Border.Effect>

        <StackPanel Orientation="Horizontal">
            <Ellipse Width="16" Height="16" StrokeThickness="2" Margin="0,0,10,0" VerticalAlignment="Center">
                <Ellipse.Triggers>
                    <EventTrigger RoutedEvent="Loaded">
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetName="SpinnerRotation"
                                                  Storyboard.TargetProperty="Angle"
                                                  From="0" To="360" Duration="0:0:0.8"
                                                  RepeatBehavior="Forever" />
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </Ellipse.Triggers>
                <Ellipse.Stroke>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <LinearGradientBrush.RelativeTransform>
                            <RotateTransform x:Name="SpinnerRotation" CenterX="0.5" CenterY="0.5" Angle="0" />
                        </LinearGradientBrush.RelativeTransform>
                        <GradientStop Color="{DynamicResource AccentColor}" Offset="0.0" />
                        <GradientStop Color="{DynamicResource SecondaryColor}" Offset="0.5" />
                        <GradientStop Color="{DynamicResource HighlightColor}" Offset="1.0" />
                    </LinearGradientBrush>
                </Ellipse.Stroke>
            </Ellipse>

            <TextBlock Text="Tekst herkennen…" Foreground="White" FontSize="13" VerticalAlignment="Center" />
        </StackPanel>
    </Border>
</Window>
